class Evento {
    constructor(id, fecha, descripcion, idUsuario, idTipoEvento) {
        this.id = id;
        this.fecha = fecha;
        this.descripcion = descripcion;
        this.idUsuario = idUsuario;
        this.idTipoEvento = idTipoEvento;
    }
}
export default Evento;
